// Test stub for the `server-only` package. In production Next.js resolves the
// real module, which throws if imported into a client bundle. Under Vitest we
// want to import server-only files directly, so this no-op stands in.
export {};
